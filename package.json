{
  "name": "pdf-chatgpt-batch",
  "version": "0.3.0",
  "description": "Headless batch driver: adapt a synthetic-document generator to directories of scanned PDFs via an agentic, role-separated ChatGPT workflow, and download the generated files",
  "license": "MPL-2.0",
  "type": "module",
  "main": "run-batch.mjs",
  "scripts": {
    "batch": "node run-batch.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "pino": "^10.3.1"
  }
}
