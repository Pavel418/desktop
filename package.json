{
  "name": "pdf-chatgpt-batch",
  "version": "0.3.0",
  "description": "Headless batch driver: run directories of PDFs through ChatGPT (with a per-directory Python template + schema chaining) and download the generated files",
  "license": "MPL-2.0",
  "type": "module",
  "main": "run-batch.mjs",
  "scripts": {
    "batch": "node run-batch.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
